{% extends 'base.html.twig' %}

{% block title %}Artisan index
{% endblock %}

{% block body %}

	{# <h1>Artisan index</h1>
						 <table class="table">
						        <thead>
						            <tr>
						                <th>Id</th>
						                <th>Nom</th>
						                <th>Description</th>
						                <th>Adresse</th>
						                <th>Numero</th>
						                <th>actions</th>
						            </tr>
						        </thead>
						        <tbody>
						        {% for artisan in artisans %}
						            <tr>
						                <td>{{ artisan.id }}</td>
						                <td>{{ artisan.nom }}</td>
						                <td>{{ artisan.description }}</td>
						                <td>{{ artisan.adresse }}</td>
						                <td>{{ artisan.numero }}</td>
						                <td>
						                    <a href="{{ path('app_artisan_show', {'id': artisan.id}) }}">show</a>
						                    <a href="{{ path('app_artisan_edit', {'id': artisan.id}) }}">edit</a>
						                </td>
						            </tr>
						        {% else %}
						            <tr>
						                <td colspan="6">no records found</td>
						            </tr>
						        {% endfor %}
						        </tbody>
						    </table> #}

	{% if is_granted('ROLE_ADMIN') %}
		{{include ('components/artisanAllAdmin.html.twig',{'artisans':artisans})}}
	{% else %}
		{{include ('components/artisanAll.html.twig',{'artisans':artisans})}}
	{% endif %}

	{# <a href="{{ path('app_artisan_new') }}">Create new</a> #}
{% endblock %}
